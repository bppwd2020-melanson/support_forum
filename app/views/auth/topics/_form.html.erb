<%= form_with(model: auth_topic, local: true) do |form| %>
  <% if auth_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auth_topic.errors.count, "error") %> prohibited this auth_topic from being saved:</h2>

      <ul>
      <% auth_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
